{
  "name": "jant-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "jant migrate --local && wrangler dev",
    "deploy": "node ../../scripts/demo-shared/run-with-env.mjs --site demo -- pnpm exec jant migrate --remote && node ../../scripts/demo-shared/run-with-env.mjs --site demo -- pnpm exec wrangler deploy",
    "reset-password": "jant reset-password",
    "export": "jant export"
  },
  "dependencies": {
    "@jant/core": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
